<%= form_with(model: client, :html => {:class => "form-horizontal center"} ,  local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field form-group row">
    <%= form.label :Nombre, class: "col-1" %>
    <%= form.text_field :name, class: "form-control col-6"%>
  </div>

  <div class="field form-group row">
    <%= form.label :Contacto, class: "col-1" %>
    <%= form.text_field :phone, class: "form-control col-6" %>
  </div>

  <div class="field form-group row">
    <%= form.label :Email, class: "col-1" %>
    <%= form.text_field :email, class: "form-control col-6" %>
  </div>

  <br>
  <div class="actions">
    <%= form.submit "Crear cliente", class: "btn"%>
  </div>
<% end %>
