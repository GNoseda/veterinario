<%= form_with(model: [@client, pet], :html => {:class => "form-horizontal center"} , local: true) do |form| %>
  <% if pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% pet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group row">
    <%= form.label :name, class: "col-1"  %>
    <%= form.text_field :name, class: "form-control col-6" %>
  </div>

  <div class="field form-group row">
    <%= form.label :race, class: "col-1"  %>
    <%= form.text_field :race, class: "form-control col-6" %>
  </div>

  <div class="field form-group row">
    <%= form.label :birthdate, class: "col-1"  %>
    <%= form.date_select :birthdate, class: "form-control col-6" %>
  </div>

  <div class="actions">
    <%= form.submit "Registrar mascota", class: "btn" %>
  </div>
<% end %>
